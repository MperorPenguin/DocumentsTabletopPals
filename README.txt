# Tabletop Pals Starter

This is a starter package for generating map indexes on Windows.

## Usage

1. Install Node.js (if not already installed).
2. Place your PNG/JPG/SVG/WEBP maps into `assets/maps/`.
3. Run:

   ```bash
   npm run maps
   ```

   This will regenerate `assets/maps/index.json`.

